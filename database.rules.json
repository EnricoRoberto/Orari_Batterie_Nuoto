{
  "rules": {
    "session": {
      ".read": true,
      ".write": "newData.child('_pin').val() === 'CAMBIAMI'"
    },
    "monitorati": {
      "$clientId": {
        ".read": true,
        ".write": true
      }
    }
  }
}
